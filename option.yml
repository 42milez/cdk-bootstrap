option:
  cdk:
    --app: 'APP'                   # path of CDK application’s main stack
    --env: 'STACK_ENV'             # Environment
    --region: 'AWS_DEFAULT_REGION' # AWS Region
    --stack: 'STACK'               # Stack Name
